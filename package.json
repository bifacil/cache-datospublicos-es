{
  "name": "cache-datospublicos-worker",
  "version":"0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --remote",
    "deploy": "npm version patch && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241022.0",
    "wrangler": "^4.43.0"
  },
  "dependencies": {
    "@bifacil/crono-json": "^0.0.2",
    "jsonpath-plus": "^10.0.1"
  }
}
